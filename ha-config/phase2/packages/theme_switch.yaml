# Phase 2.6.3: theme switching now handled entirely by HA's built-in
# prefers-color-scheme Auto mode on the merged "Casa Cola Creek" theme.
# No automations, scripts, or input_selects needed here.
#
# To enable: in /profile set Theme = "Casa Cola Creek" and Mode = Auto.
#
# This package file is intentionally near-empty; kept so existing deploy
# scripts that reference it don't fail.

# (no entities)
